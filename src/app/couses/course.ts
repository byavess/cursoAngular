export interface Course {
    id: number,
    name: string,
    imageUrl: string,
    price: number,
    code: string,
    duration: number,
    releaseDate : Date,
    rating: number,
    description: string,
}