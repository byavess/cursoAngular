export interface Course {
    id: number,
    nome: string,
    imageUrl: string,
    price: number,
    code: string,
    duration: number,
    releaseDate : Date,
    rating: number,
    description: string,
}